package com.example.pageone


import org.junit.Test
import org.junit.Assert.*

/**
 * Example local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
    //private val dbHelper = DatabaseHelper(ApplicationProvider.getApplicationContext())

    /*@Test
    fun testCalculateVehiclePrice() {
        val expectedPrice = 3000
        val actualPrice = dbHelper.calculateVehiclePrice("car")
        assertEquals(expectedPrice, actualPrice)
    }*/
}
